import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
    logo: {
        width: 250,
        height: 250,
        marginBottom: -20,
        zIndex: 0
    },

    linkToScreensContainer: {
        width: '80%',
        marginTop: 40,
        alignItems: 'center'
    }
});